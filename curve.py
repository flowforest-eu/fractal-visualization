class Curve:
    def __init__(self, x_orig, y_orig, scale, angle_deg, mirror):
        self.x_orig = x_orig
        self.y_orig = y_orig
        self.scale = scale
        self.angle_deg = angle_deg
        self.mirror = mirror
